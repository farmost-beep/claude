# CLAUDE.md — Karpathy 编码原则

## Karpathy 四条原则

1. **先想清楚再写** — 不确定就问，别猜。有多个方案就列出，选最简单的说原因。
2. **保持简单** — 只解决被要求的问题。不加抽象、不加扩展、不加未来可能用到的功能。
3. **手术刀式改动** — 只碰要改的地方。不改格式、不改相邻代码、不改没坏的东西。
4. **目标驱动** — 每个改动要能验证：写测试证明 bug 修了，跑一遍确认 PDF 生成了。多步骤任务每步写一个验证点。
